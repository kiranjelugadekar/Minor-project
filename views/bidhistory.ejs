<% layout('boilerplate')%>
<%- include('./includes/navbar') %>
 <section id="dashboardSection" class="app-section active z-1">

<h3 style="color: #1a6672; margin-left: 5rem;">Bid History (Ended Auctions)</h3>

<div class="container mt-4">
    
    <% if (products.length > 0) { %>
        <div class="row g-4">
            <% products.forEach(product => { %>
                <div class="col-md-4 mb-4">
                    <div class="card h-100 product-card ended">
                        <div class="card-body">
                            <h5 class="card-title"><%= product.name %></h5>
                            <p class="card-text"><%= product.description %></p>
                            <div class="bid-info mb-2">
                                <div class="current-bid">
                                    <strong>Bid Amount:</strong>
                                    <span class="text-success fs-5">
                                        ₹<%= product.winningBid %>
                                    </span>
                                </div>
                                <small class="text-muted">
                                    winner is <%= product.lastBidder.username %> 
                                </small>
                            </div>
                        </div>
                        <div class="card-footer bg-transparent">
                            <span class="badge bg-danger">Auction Ended</span>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    <% } else { %>
        <div class="alert alert-info" style="margin-left: 2rem;">No ended auction products found.</div>
    <% } %>
</div>
</section>
<style>

.card {
    transition: transform 0.2s, box-shadow 0.2s;
}
.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
}
.product-card.ended {
    border: 2px solid #c0212f;
    background: #fff0f0;
}
.badge.bg-danger {
    background-color: #c0212f;
    color: #fff;
    font-size: 1rem;
    padding: 0.5em 1em;
    border-radius: 0.5em;
}
</style>